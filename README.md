'Start by creating a repository using the github interface and then create the same directory in your local machine using your OS command line interface. Intialize git and the add the remote origin' 
